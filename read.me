# 🎤 Video Transcriber with Chat Q&A (Flask + Whisper + Local LLM)

This is a web application that allows you to upload a video file, transcribe it using OpenAI's Whisper, and ask questions about the transcript using a local LLM (e.g., LM Studio running `deepseek-r1-distill-qwen-7b`).

---

## 📦 Requirements

- Python 3.8+
- ffmpeg installed and available in PATH
- Whisper model dependencies
- Local LLM API (e.g., LM Studio running locally at `http://127.0.0.1:1234`)

---

## 🚀 Installation

### 1. Clone this repository

```bash
git clone https://github.com/your-username/video-transcriber-web.git
cd video-transcriber-web
